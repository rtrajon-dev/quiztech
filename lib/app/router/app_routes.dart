/// Centralized route path + name constants.
class AppRoutes {
  // Auth
  static const login = '/login';

  // Main app — tab shell branches
  static const home = '/home';
  static const score = '/score';
  static const profile = '/profile';

  // Quiz flow (pushed over the shell, no bottom nav)
  static const quizDetails = '/quiz-details';
  static const quizPlay = '/quiz-play';
}
